package me.hyperperform.rest;

import org.jboss.resteasy.client.ClientRequest;
import org.jboss.resteasy.client.ClientResponse;

import org.junit.Assert;
import org.junit.Test;

public class RestTest
{
	// static String url = "localhost:8080/hyperperform-system-1.0-SNAPSHOT/rs/";

	@Test
	public void simpleTest() throws Exception
	{
		// ClientRequest req = new ClientRequest(url + "demo");
		// ClientResponse<String> res = req.get(String.class);

		// Assert.assertNotNull(res);
		// Assert.assertEquals(res, "Hello World!");
	}
}